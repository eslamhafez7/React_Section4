// myReactApp.jsx
// Notes.jsx
import React from 'react';
import ReactDOM from 'react-dom';

const Test = () => { // Capitalized the function name
  return (
    <div>
      <h1>Hello World</h1>
    </div>
  );
}

ReactDOM.render(<Test />, document.getElementById('root')); // Use JSX syntax to render the component



ReactDOM.render(test, document.getElementById('root'));

